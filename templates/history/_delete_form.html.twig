<form method="post" action="{{ path('history_delete', {'id': history.id}) }}" onsubmit="return confirm('Etes vous sûre de vouloir supprimer cet item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ history.id) }}">
    <button class="btn btn-danger">Effacer</button>
</form>
