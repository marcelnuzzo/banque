{% extends 'admin/base.html.twig' %}

{% block title %}Compte en banque{% endblock %}

{% block body %}
    <h1>Compte en banque</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bankaccount.id }}</td>
            </tr>
            <tr>
                <th>Iban</th>
                <td>{{ bankaccount.iban }}</td>
            </tr>
            <tr>
                <th>Amount</th>
                <td>{{ bankaccount.amount }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('admin_bankaccount_index') }}">Retour à la liste</a>

    <a href="{{ path('admin_bankaccount_edit', {'id': bankaccount.id}) }}">Editer</a>

    {{ include('admin/bankaccount/_delete_form.html.twig') }}
{% endblock %}
